# 数据治理平台 - 流程图决策记录

> **文档用途**：记录业务流程设计过程中的关键决策及依据
> **原则**：说清楚为什么这样决策

**创建日期**：2026-03-28
**涉及文档**：`数据治理平台-Mermaid.mmd`

---

## 一、业务场景边界决策

### 1.1 双场景独立定位

**决策结论**：数据治理平台不替代巡查整改流程，两个场景独立运作。

**决策理由**：
- 巡查整改是成熟业务，有既定交付要求（整改报告给保险公司）
- 数据治理是新业务，目标是积累AI训练数据
- 强行合并会导致两边都做不好：巡查员负担增加，数据治理也被业务节奏绑架
- 先让数据治理平台作为"副产物加工者"跑起来，验证价值后再考虑融合

**决策影响**：
- 数据治理平台的输入就是巡查场景的"原始图片副本"
- 平台专注做"标注治理"，不介入前置质量把控

---

### 1.2 平台不做任何预筛选

**决策结论**：数据治理平台不做任何前置筛选（包括质量检测、去重等），原始图片直接进入 S4 标注环节。

**决策理由**：
- **责任边界**：图片质量是巡查团队/保险公司验收的责任，不是数据治理平台的职责
- **MVP极简**：Phase 1 目标是跑通"采集→标注→入库"完整闭环，非核心功能全部后置
- **问题后置**：同场景重复拍照、图片模糊等问题，由标注员在 S4 环节判断，标记后废弃

**决策影响**：
- 标注员会收到"裸图"，需要在 S4 环节判断图片是否可用、是否重复
- 极模糊/明显无效/重复的图片由标注员标记废弃，不进入训练集

---

## 二、采集端设计决策

### 2.1 小程序主推，相机过渡

**决策结论**：MVP阶段主推小程序拍照，但允许原生相机+批量导入并行，不强制一刀切。

**决策理由**：
- **Phase 2铺垫**：未来要在平台生成整改报告，必须让巡查员提前习惯用小程序拍照
- **降低阻力**：巡查员多年习惯用相机，强行切换会导致抵触，采用"引导而非强制"策略
- **数据对齐**：两种方式最终都流入同一平台，不分裂数据池

**决策影响**：
- 小程序只需支持"拍照上传"，暂不需要替代相机所有功能
- 相机方式作为"兜底"保留，但运营上引导向小程序迁移

---

### 2.2 小程序功能极简（零标注）

**决策结论**：小程序只做三件事：进入项目、批量拍照、自动上传。不做任何现场标注或信息填写。

**决策理由**：
- **配合度优先**：巡查员是"我们自己的人"，但配合度需要维护；每多一个步骤，流失率上升
- **核心路径≤3步**：进入项目→拍照→完成，现场操作越快越好
- **信息后置**：隐患类型、法规关联等专业判断，交给后端标注员更专业；巡查员现场判断容易错，反而污染数据

**舍弃的功能及原因**：
| 功能 | 舍弃原因 | 替代方案 |
|------|---------|---------|
| 现场隐患标注 | 增加负担，降低配合度 | 后置到S4标注环节 |
| 语音描述 | Phase 1暂不需要，保持极简 | Phase 2生成报告时考虑 |
| 实时质量检测 | 不是数据治理平台职责 | 巡查团队自行把控 |

---

### 2.3 照片必须与项目关联

**决策结论**：所有照片必须关联到具体项目，但关联方式灵活（扫码/列表选择/事后补录）。

**决策理由**：
- **标注质量依赖**：项目类型（家装/小型工程/零星作业）是隐患类型预测、法规关联的强特征
  - 例："电线裸露"在家装和工地的法规条款完全不同
  - 不关联项目，标注员无法判断场景，法规关联错误率飙升
- **备案系统已有数据**：既然是我们自建的备案系统，项目数据唾手可得，不利用是浪费

**关联方式设计**：
| 方式 | 优先级 | 使用场景 |
|------|--------|---------|
| 扫现场二维码 | 推荐 | 现场有码且清晰，最快捷 |
| 项目列表选择 | 备选 | 无二维码或网络差 |
| 事后补录 | 兜底 | 特殊情况，24小时内运营补关联 |

**技术实现**：
- 一次进入项目，批量拍照自动继承项目ID
- 照片带项目ID水印，S4标注员可见项目类型信息

---

### 2.4 小程序拍照流程与交互设计

**决策结论**：采用 CameraContext 自定义相机方案，实现连拍+待确认+离线存储的完整流程。

**决策理由**：
- **wx.chooseMedia 不满足需求**：系统相机拍一张就返回，不支持连续拍摄，体验割裂
- **现场场景需要连拍**：一处隐患通常需要2-3张不同角度，连拍减少操作步骤
- **防错机制必要**：拍完立即确认，避免误拍、漏拍流入后端浪费标注人力
- **离线场景必须支持**：施工现场网络信号不稳定，不能因网络问题阻断采集

**页面流程设计**：
```
项目详情页 → [上传图片] → 选择方式
                           ├─ [现场拍照] → 拍照页（CameraContext）→ [查看] → 待确认上传页 → [确认上传] → 上传成功
                           └─ [从相册选择] → 相册选择页 → 选择多张 → 待确认上传页 → [确认上传] → 上传成功
```

**项目详情页入口设计**：
| 入口 | 功能 | 使用场景 |
|------|------|---------|
| [现场拍照] | 进入 CameraContext 连拍 | 在现场，需要实时拍摄 |
| [从相册选择] | 调用 wx.chooseMedia 选择本地图片 | 已用相机拍照，需批量补传 |

**拍照页功能清单**：
| 功能 | 说明 | 必要性 |
|------|------|--------|
| 相机预览区域 | CameraContext 实时预览 | 核心 |
| 拍照按钮 | 点击拍照，拍完后停留在当前页可继续拍 | 核心 |
| 已拍张数提示 | 顶部显示"已拍 3 张" | 必要 |
| 查看按钮 | 点击进入待确认页，查看/管理已拍照片 | 核心 |
| 返回/取消 | 放弃本次拍照，返回项目详情页 | 必要 |
| 当前项目提示 | 顶部显示项目名称，防拍错项目 | 必要 |

**待确认上传页功能清单**：
| 功能 | 说明 |
|------|------|
| 网格式平铺预览 | 所有照片以网格形式平铺展示 |
| 全屏查看 | 点击单张图片进入全屏放大查看 |
| 全屏删除 | 全屏查看页面底部显示删除按钮，可直接删除该张 |
| 返回预览 | 全屏查看页面可返回网格预览页 |
| 长按多选 | 长按某张图片进入多选模式，可多选图片 |
| 批量删除 | 多选后一键删除选中图片 |
| 继续拍照 | 返回拍照页追加拍摄 |
| 确认上传 | 批量上传至平台（至少1张才可点击）|
| 取消放弃 | 二次确认后放弃本次所有照片 |

**离线存储方案**：
| 场景 | 处理逻辑 |
|------|---------|
| 有网络 | 拍照 → 直接上传 → 成功提示 |
| 无网络 | 拍照 → 本地文件系统存储 → 标记"待上传" |

**本地存储结构**：
```
StorageKey: "pending_uploads_${projectId}"
{
  projectId: "xxx",
  photos: [
    {path: "wxfile://tmp_xxx.jpg", time: 1711593600, lat: 31.2, lng: 121.5},
    ...
  ],
  createdAt: 1711593600
}
```

**网络恢复后处理**：
- 小程序启动时自动检测：有待上传任务 → 提示"有X张未上传照片"
- 项目详情页显示"待上传"徽章，点击进入待传列表手动上传
- 支持断点续传，上传完成自动清理本地文件

**边界情况处理**：
| 情况 | 处理 |
|------|------|
| 拍了0张点查看 | 按钮置灰，提示"请至少拍摄1张" |
| 拍照中退出小程序 | 本地缓存已拍图片，下次进入提示"有未上传照片，是否继续" |
| 存储空间不足 | 提示"手机存储空间不足，请先清理" |
| 相机权限被拒绝 | 引导去设置页开启权限 |
| 上传中断 | 支持断点续传或重新上传 |
| 单次张数上限 | 限制50张，防止误触导致内存溢出 |
| 本地存储上限 | 100MB或50张，超出提示清理历史照片 |

---

### 2.5 图片水印与元数据方案

**决策结论**：不加可见水印，使用 EXIF 元数据存储时间/位置/项目ID信息。

**决策理由**：
- **可见水印影响AI训练**：时间戳、文字等像素叠加会被模型学到，成为干扰特征
- **像素纯净原则**：AI训练只认像素，元数据不影响像素，训练数据更干净
- **溯源需求可满足**：时间、GPS、项目ID写入EXIF，满足后续追溯需要

**元数据内容**：
| 字段 | 来源 | 用途 |
|------|------|------|
| 拍摄时间 | 系统时间 | 追溯拍摄时点 |
| GPS位置 | 微信定位API | 确认现场位置（如有权限）|
| 项目ID | 小程序当前项目 | 关联到具体项目，S4标注展示 |
| 巡查员ID | 登录用户ID | 追溯责任人 |

**标注员视角展示**：
```
┌─────────────────────────────┐
│ 隐患图片（纯净无水印）        │
│                             │
│ 侧边信息栏：                 │
│ • 项目：家装/XX小区          │
│ • 时间：2026-03-28 14:30    │
│ • 位置：GPS坐标（如有）      │
│                             │
│ [隐患类型选择]               │
│ [法规关联]                   │
└─────────────────────────────┘
```

**技术实现**：
- 拍照时使用 `wx.getLocation` 获取GPS（需用户授权）
- 使用 `wx.getFileSystemManager` 写入EXIF（或上传时后端写入）
- 标注界面从数据库读取元数据展示，不依赖图片本身

---

## 三、数据同步机制决策

### 3.1 备案系统单向同步

**决策结论**：备案系统→数据治理平台，单向同步，每日凌晨2:00自动执行+后台手动触发。

**决策理由**：
- **数据源唯一**：备案系统是项目信息的权威来源，平台只消费不修改
- **避免双向同步复杂性**：双向同步需要处理冲突、时序等问题，MVP阶段不值得投入
- **批量下发场景**：保险公司可能集中下发一批事故预防服务订单，需要同步一批新项目，手动触发满足灵活性

**同步配置**：
| 配置项 | 默认值 | 可调整性 |
|--------|--------|---------|
| 频率 | 每日凌晨2:00 | 后台可改 |
| 触发 | 自动+手动并行 | 自动失败可人工补 |
| 范围 | 全部活跃项目 | 可按状态筛选 |

---

### 3.2 同步数据直接落地使用

**决策结论**：备案系统同步的数据，直接写入后台管理系统项目列表页并推送至小程序，不做筛选或确认。

**决策理由**：
- **备案侧全权管理**：项目数据的质量、有效性完全由备案系统负责，数据治理平台不做干预
- **被动接收原则**：平台只消费备案系统推送的数据，不做"脏数据"判断或过滤
- **减少中间环节**：去掉运营确认环节，避免人为延迟，巡查员可即时看到最新项目

**数据流转**：
```
备案系统 → 自动/手动同步 → 后台管理系统项目列表页 + 推送小程序
              ↓
         失败告警 → 人工介入（技术层面，非数据审核）
```

**异常处理**：
- 同步失败（网络/接口问题）：技术告警，人工修复同步机制
- 数据内容问题（项目错误/重复）：反馈给备案侧处理，平台不做本地修正

---

**下次更新时机**：S4-S5标注机制讨论完成后
