# 数据治理平台 - 流程图决策记录

> **文档用途**：记录业务流程设计过程中的关键决策及依据
> **原则**：说清楚为什么这样决策，每个主题只在一处完整讨论

**创建日期**：2026-03-28
**涉及文档**：`数据治理平台-Mermaid.mmd`

---

## 一、项目来源决策

### 1.1 项目数据来源

**决策结论**：项目数据有两个来源，备案系统同步为增值功能（能通则通），后台手动创建为保底功能（必须实现）。

**来源一：备案系统同步（增值功能）**

| 维度 | 说明 |
|------|------|
| 数据内容 | 项目全量信息（名称、类型、地址、备案编号等）|
| 同步频率 | 每日凌晨2:00自动执行 + 后台手动触发 |
| 前置条件 | 需政府部门确认数据对接可行性，可能需签署数据共享协议 |

**来源二：后台手动创建（保底功能）**

无论备案系统能否对接，后台管理系统必须支持手动创建项目：

| 场景 | 操作流程 |
|------|---------|
| 备案系统可同步 | 自动同步为主，后台可人工增补/修正 |
| 备案系统不可同步 | 运营根据保险公司服务订单或自行维护的数据，在后台手动录入 |

**后台创建项目字段**：
| 字段 | 必填 | 说明 |
|------|------|------|
| 项目名称 | 是 | 如"万科云城三期12栋" |
| 项目类型 | 是 | 家装/小型工程/零星作业（影响后续标注）|
| 施工内容 | 否 | 如"外墙装修" |
| 项目地址 | 是 | 用于GPS校验（可选）|
| 巡查日期 | 否 | 计划巡查时间 |
| 备案编号 | 否 | 如有则填入，用于对照 |
| 保险公司 | 是 | 关联服务订单来源 |

**项目创建后流向**：
```
后台创建项目 → 写入项目列表页 → 同步至小程序项目列表 → 巡查员可选择关联
```

---

### 1.2 备案系统同步风险与应对

**关键风险：政府侧数据外流限制**

| 风险项 | 说明 | 影响 |
|--------|------|------|
| 备案系统部署在政府侧服务器 | 备案数据含用户敏感信息，政府可能不允许数据同步到互联网平台 | 无法自动同步项目列表，小程序无法扫码/选择项目关联 |

**风险应对预案**：

| 情况 | 应对方案 |
|------|---------|
| 允许数据同步 | 按原计划执行，每日同步备案项目数据 |
| 不允许同步，但允许脱敏查询 | 小程序扫码/搜索时实时查询政府侧接口，不落库 |
| **完全不允许对接** | **改用保险公司指派项目预录入** |

**保险公司预录入方案（备案系统不可对接时的替代）**：

| 环节 | 说明 |
|------|------|
| 数据来源 | 保险公司事故预防服务订单（含项目地址、类型、施工内容）|
| 录入方式 | 运营人员在后台预录入，创建项目基本信息 |
| 同步时机 | 巡查员出发前，项目已存在于平台，小程序可选择 |

**方案对比**：
| 维度 | 备案系统同步 | 保险公司预录入 |
|------|-------------|----------------|
| 数据获取难度 | 需政府审批，难度高 | 保险公司合作方，易获取 |
| 数据完整性 | 完整（含备案全量信息）| 依赖保险公司提供字段 |
| 运营工作量 | 低（自动同步）| 高（需人工录入）|
| 合规风险 | 有（政务数据外流）| 低（商业合作数据）|
| 巡查员体验 | 扫码/选择项目 | 相同 |
| 标注质量 | 高（项目类型准确）| 高（保险公司提供类型）|

---

### 1.3 项目列表推送至小程序

**决策结论**：项目数据直接写入后台管理系统项目列表页并推送至小程序，不做筛选或确认。

**决策理由**：
- **备案侧全权管理**：项目数据的质量、有效性完全由备案系统负责，数据治理平台不做干预
- **被动接收原则**：平台只消费备案系统推送的数据，不做"脏数据"判断或过滤
- **减少中间环节**：去掉运营确认环节，避免人为延迟，巡查员可即时看到最新项目

**数据流转**：
```
备案系统/后台创建 → 自动同步 → 后台管理系统项目列表页 + 推送小程序
                              ↓
                        失败告警 → 人工介入（技术层面，非数据审核）
```

**异常处理**：
- 同步失败（网络/接口问题）：技术告警，人工修复同步机制
- 数据内容问题（项目错误/重复）：反馈给备案侧处理，平台不做本地修正

---

## 二、采集端设计决策

### 2.1 小程序整体流程设计

**用户旅程**：一键登录 → 项目列表 → 项目详情 → 图片上传

---

#### 2.1.1 登录与项目列表页

**决策结论**：用户微信一键登录后，进入项目列表页，显示所有待巡查项目（全员列表，暂不分角色）。

**项目列表页设计**：

| 展示字段 | 说明 |
|---------|------|
| 项目名称 | 如"万科云城三期12栋" |
| 项目地点 | 项目地址简化显示 |
| 巡查状态 | 未巡查 / 已巡查 |

**状态显示规则**：

| 状态 | 展示方式 |
|------|---------|
| **未巡查** | 显示"未巡查"标签（视觉重点标注，如红色/橙色标签）|
| **已巡查** | 显示"巡查人：[姓名]，巡查时间：[时间]" |

**关键设计点**：
- 一个项目支持多次巡查，列表页显示最近一次巡查信息
- 未巡查项目排在列表前列或视觉突出，便于巡查员识别
- 点击项目卡片进入项目详情页

---

#### 2.1.2 项目详情页

**页面内容**：

| 区块 | 内容 |
|------|------|
| 项目基本信息 | 项目名称、项目类型、施工内容、项目地址、巡查日期 |
| 图片上传入口 | 从相册选择（MVP阶段）|
| 待上传区域 | 显示待上传图片数量（如有）|
| 已上传区域 | 显示已上传图片数量，可点击查看 |
| **巡查记录** | 按时间轴展示历史巡查 |

**巡查记录设计（时间轴）**：

| 字段 | 说明 |
|------|------|
| 巡查人 | 执行巡查的人员姓名 |
| 巡查时间 | 巡查完成的日期时间 |
| 上传图片数量 | 该次巡查上传的图片张数 |

**时间轴示例**：
```
┌─────────────────────────────┐
│  巡查记录                    │
│                             │
│  ● 2026-03-28 14:30         │
│  │ 巡查人：张三              │
│  │ 上传图片：12张 →          │
│  │                           │
│  ● 2026-03-15 09:00         │
│  │ 巡查人：李四              │
│  │ 上传图片：8张 →           │
│                             │
└─────────────────────────────┘
```

---

### 2.2 小程序功能极简（零标注）

**决策结论**：小程序只做三件事：进入项目、选择图片、批量上传。不做任何现场标注或信息填写。

**决策理由**：
- **配合度优先**：巡查员是"我们自己的人"，但配合度需要维护；每多一个步骤，流失率上升
- **核心路径≤3步**：进入项目→拍照→完成，现场操作越快越好
- **信息后置**：隐患类型、法规关联等专业判断，交给后端标注员更专业；巡查员现场判断容易错，反而污染数据

**舍弃的功能及原因**：
| 功能 | 舍弃原因 | 替代方案 |
|------|---------|---------|
| 现场隐患标注 | 增加负担，降低配合度 | 后置到S4标注环节 |
| 语音描述 | Phase 1暂不需要，保持极简 | Phase 2生成报告时考虑 |
| 实时质量检测 | 不是数据治理平台职责 | 巡查团队自行把控 |

---

### 2.3 照片必须与项目关联

**决策结论**：所有照片必须关联到具体项目，但关联方式灵活（扫码/列表选择/事后补录）。

**决策理由**：
- **标注质量依赖**：项目类型（家装/小型工程/零星作业）是隐患类型预测、法规关联的强特征
  - 例："电线裸露"在家装和工地的法规条款完全不同
  - 不关联项目，标注员无法判断场景，法规关联错误率飙升
- **备案系统已有数据**：既然是我们自建的备案系统，项目数据唾手可得，不利用是浪费

**关联方式设计**：
| 方式 | 优先级 | 使用场景 |
|------|--------|---------|
| 扫现场二维码 | 推荐 | 现场有码且清晰，最快捷 |
| 项目列表选择 | 备选 | 无二维码或网络差 |
| 事后补录 | 兜底 | 特殊情况，24小时内运营补关联 |

**技术实现**：
- 一次进入项目，批量上传自动继承项目ID
- S4标注员可见项目类型信息（水印方案详见2.7节，待确认）

---

### 2.4 小程序上传方式优先级决策

**决策结论**：MVP阶段优先实现"方式2（原相机拍照 + 小程序批量上传）"，"方式1（小程序现场拍照）"后置到Phase 2。

**决策理由**：
- **技术复杂度对比**：方式1涉及断网存储、上传续传、连拍管理、本地缓存清理等复杂逻辑；方式2只需调用 wx.chooseMedia 选择相册图片，技术简单
- **页面数量对比**：方式1需要拍照页、待确认页、离线管理页等；方式2只需项目详情页 + 相册选择器
- **边界情况**：方式1需处理网络中断、存储空间不足、后台上传、权限被拒等多种异常；方式2异常场景少
- **用户习惯匹配**：巡查员已习惯原相机拍照，方式2不改变拍摄习惯，只优化上传环节

**三种方式定位**：
| 方式 | 描述 | MVP阶段 | 占比预测 | 理由 |
|------|------|---------|---------|------|
| **方式2：原相机+小程序批量上传** | 相机拍照 → 小程序选相册 → 批量上传 | **必须实现** | 70% | 技术简单，最符合巡查员 workflow |
| **方式3：后台批量导入** | 相机拍照 → 事后电脑登录后台 → 选项目 → 上传 | **保留可用** | 25% | 兜底方式，老巡查员过渡用 |
| **方式1：小程序现场拍照** | 小程序内连拍 → 待确认 → 上传 | **后置到Phase 2** | 5% | 技术复杂，MVP阶段投入产出比低 |

**方式2详细流程**：
```
项目详情页 → [从相册选择] → 微信相册选择器（wx.chooseMedia）→ 选择多张 → 自动上传 → 上传成功
```

**方式2功能清单**：
| 功能 | 说明 | 必要性 |
|------|------|--------|
| 相册选择入口 | 项目详情页显示"从相册选择"按钮 | 核心 |
| 批量选择 | 支持选择多张图片（建议上限20张） | 核心 |
| 自动上传 | 选择后直接上传，无确认页 | 核心 |
| 上传进度提示 | 显示"上传中 3/20" | 必要 |
| 失败重试 | 单张失败可重试，或标记待处理 | 必要 |
| 网络异常提示 | 无网络时提示"请检查网络后重试" | 必要 |

**项目详情页设计（MVP阶段）**：
```
┌─────────────────────────────┐
│ ← 项目详情              ... │
├─────────────────────────────┤
│                             │
│  万科云城三期12栋           │
│  ├─ 项目类型：小型工程      │
│  ├─ 施工内容：外墙装修      │
│  ├─ 项目地址：南山区XX路    │
│  └─ 巡查日期：2026-03-28    │
│                             │
├─────────────────────────────┤
│  上传图片                    │
│                             │
│       ┌─────────┐          │
│       │  🖼️    │          │
│       │         │          │
│       │ 从相册  │          │
│       │ 选择    │          │
│       └─────────┘          │
│                             │
│  🔴 待上传：3张（35MB）→    │
│                             │
├─────────────────────────────┤
│  已上传：15张  →            │
│                             │
│  （点击进入查看，不可删除）   │
│                             │
└─────────────────────────────┘
```

**已上传图片查看页**：
```
┌─────────────────────────────┐
│ ← 已上传图片（15张）        │
├─────────────────────────────┤
│                             │
│  ┌─────────────────────┐   │
│  │                     │   │
│  │    当前图片大图      │   │
│  │    （左右滑动切换）  │   │
│  │                     │   │
│  │    3 / 15           │   │
│  │                     │   │
│  └─────────────────────┘   │
│                             │
│  2026-03-28 14:30           │
│                             │
└─────────────────────────────┘
```

**功能说明**：
| 区域 | 交互 | 说明 |
|------|------|------|
| 项目详情页 | 显示"已上传：X张" | 仅展示数量，不占空间 |
| 点击进入 | 全屏查看大图 | 左右滑动切换图片 |
| 查看页 | 不可删除 | 已入库数据，删除需走后台流程 |

**说明**：MVP阶段仅保留"从相册选择"，现场拍照功能后置到Phase 2。

**页面元素说明**：
| 区域 | 元素 | 功能 |
|------|------|------|
| 顶部 | 返回箭头 + 标题 + 更多 | 导航 |
| 项目信息 | 名称/类型/内容/地址/日期 | 确认当前项目 |
| 上传入口 | 现场拍照 / 从相册选择 | 两种方式并行 |
| 待上传 | 红色徽章（有数据时显示） | 进入失败记录管理页 |
| 已上传 | 缩略图网格 + 时间 | 查看历史上传 |

**失败记录管理（待上传列表页）**：
```
┌─────────────────────────────┐
│ ← 待上传记录（3张）          │
│                             │
│  ┌─────┐ ┌─────┐ ┌─────┐   │
│  │ 图1 │ │ 图2 │ │ 图3 │   │
│  │失败 │ │失败 │ │失败 │   │
│  │网络 │ │超时 │ │过大 │   │
│  └─────┘ └─────┘ └─────┘   │
│                             │
│  （点击单张可删除，长按可多选删除）│
│                             │
│  [相册选择]  [开始上传]  [清空放弃]│
└─────────────────────────────┘
```

**功能说明**：
| 功能 | 交互 | 说明 |
|------|------|------|
| 删除单张 | 点击单张图片 | 确认后删除该张 |
| 批量删除 | 长按进入多选模式 | 选择多张后一键删除 |
| 相册选择 | [相册选择]按钮 | 跳转相册选择器，追加新图片 |
| 开始上传 | [开始上传]按钮 | 上传当前列表所有图片 |
| 清空放弃 | [清空放弃]按钮 | 二次确认后删除所有待上传图片 |

**失败处理机制**：
| 场景 | 原因 | 处理策略 |
|------|------|---------|
| 网络中断 | 信号差/WiFi断开 | 自动重试3次，仍失败则标记"待上传"，存本地 |
| 单张失败 | 某张图片过大/格式异常 | 提示"X张上传失败"，可选重试或跳过 |
| 批量失败 | 服务端异常/Token过期 | 全部标记"待上传"，不丢失数据 |
| 服务端拒绝 | 重复上传/图片损坏 | 提示具体原因，用户确认后删除或重传 |

**图片规格与并发策略**：
| 配置项 | 建议值 | 说明 |
|--------|--------|------|
| 单张大小限制 | ≤10MB | 超出提示"图片过大，建议压缩后上传" |
| 批量选择上限 | 20张 | 避免一次选择过多导致内存溢出 |
| 并发上传数 | 3-5张 | 并行上传，平衡速度与稳定性 |
| 超时时间 | 30秒/张 | 超时时自动重试或标记失败 |

---

### 2.5 后台批量导入（兜底方式）

**决策结论**：保留后台批量导入功能，作为巡查员不使用小程序时的兜底方案。

**决策理由**：
- **过渡需求**：部分老巡查员习惯用电脑整理，强制切换小程序会导致抵触
- **技术简单**：后台上传组件成熟，开发成本低
- **应急备用**：小程序故障或巡查员手机没电时，有替代方案

**流程设计**：
```
相机拍照 → 事后开电脑 → 登录后台管理系统 → 选择项目 → 批量上传图片 → 关联成功
```

**功能清单**：
| 功能 | 说明 |
|------|------|
| 项目选择 | 下拉选择或搜索项目 |
| 批量选择 | 支持 Ctrl/Shift 多选图片 |
| 上传进度 | 显示上传进度条 |
| 失败重试 | 失败任务可重新上传 |

**使用占比预测**：25%，主要面向习惯电脑操作的老巡查员和应急场景。

---

### 2.6 小程序现场拍照（Phase 2 规划）

**决策结论**：方式1（小程序现场连拍）后置到Phase 2，MVP阶段仅做预留接口。

**决策理由**：技术复杂度高（断网存储、上传续传、本地缓存管理），MVP阶段优先实现更简单的方式2。

**Phase 2 功能概要**：
- 项目详情页进入现场拍照模式
- CameraContext 连拍，拍完后进入待确认页
- 支持删除、全屏查看、批量选择
- 离线存储，有网后自动/手动上传
- 可选保存到本地相册（双写）

详细设计待Phase 2启动时再细化。

---

### 2.7 图片水印方案（待确认）

**当前状态**：等待确认保险公司对水印的具体要求，两种技术方案待决策。

**方案A：强制小程序现场拍照（原方案）**

| 环节 | 说明 |
|------|------|
| **适用条件** | 保险公司要求可见水印，且必须体现在原始图片上 |
| **采集方式** | 强制使用方式1（小程序现场拍照） |
| **技术实现** | 小程序拍照时不叠加水印，原始图片上传平台；展示/导出时前端叠加 |
| **优缺点** | ✅ 训练数据绝对纯净；❌ 必须推广小程序拍照，开发复杂度高，巡查员需改变习惯 |

**方案B：统一原始图片上传，导出时叠加水印（优化方案）**

| 环节 | 说明 |
|------|------|
| **适用条件** | 保险公司要求可见水印，但接受导出时添加 |
| **采集方式** | 三种方式均可（小程序拍照/原相机+小程序上传/后台导入） |
| **技术实现** | 任意方式上传原始图片；后台管理系统导出整改报告时前端叠加水印 |
| **优缺点** | ✅ 训练数据纯净；✅ 不限制巡查员使用习惯；✅ 开发成本相对较低 |

**方案C：仅EXIF元数据（无可见水印）**

| 环节 | 说明 |
|------|------|
| **适用条件** | 保险公司接受EXIF元数据作为溯源依据，不要求可见水印 |
| **采集方式** | 三种方式均可 |
| **技术实现** | 原相机EXIF含时间/GPS，平台补充项目ID；无需叠加可见水印 |
| **优缺点** | ✅ 方式最灵活；✅ 无额外开发成本；✅ 巡查员零负担 |

**待确认事项**：
| 问题 | 影响 | 跟进时机 |
|------|------|---------|
| 保险公司是否要求**可见水印** | 如要求，需在方案A和方案B中选择；如不要求，采用方案C | 与保险公司合作洽谈时确认，优先级**高** |
| 如要求可见水印，是否接受**导出时添加** | 决定采用方案A（强制小程序）还是方案B（方式灵活） | 与保险公司合作洽谈时确认 |

---

**下次更新时机**：S4-S5标注机制讨论完成后
