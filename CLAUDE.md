# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库概览

这是一个**纯文档仓库**，用于记录数据治理平台（数据治理平台）的产品设计文档，服务于小散工程安责险事故预防服务业务。

**语言**：简体中文 - 所有文档均使用中文编写。

## 文档结构

| 文件 | 用途 |
|------|------|
| `数据治理平台-上下文.md` | 业务背景、战略定位、价值链分析和风险评估 |
| `流程图决策.md` | 产品设计决策、UI/UX设计规范、流程设计 |
| `知识库体系设计.md` | 隐患识别报告的知识库架构（隐患库、法律法规库、案例库等） |
| `数据治理平台-Mermaid.mmd` | 系统架构流程图（Mermaid语法） |

## 核心概念

理解以下术语有助于你阅读文档：

| 术语 | 含义 |
|------|------|
| **安责险** | 安全生产责任保险（高危行业强制投保） |
| **小散工程** | 小型、分散的工程项目（装修、维修等） |
| **巡查员** | 现场执行巡查、采集隐患照片的人员 |
| **S1-S8** | 系统阶段：项目来源 → 采集 → 汇聚 → 多轮标注 → 质检 → 数据池 → AI训练 |
| **标注** | 对隐患图片进行标注、分类、关联法规的数据处理工作 |
| **备案系统** | 政府侧的建设工程项目备案管理系统 |

## 文档编辑规范

1. **保持一致性** - 修改`流程图决策.md`中的流程时，应同步更新Mermaid流程图（`数据治理平台-Mermaid.mmd`）
2. **遵循现有格式** - 使用表格展示规格说明，使用ASCII图表展示UI原型，使用层级标题组织内容
3. **更新决策记录** - 更改产品决策时，在相应的决策章节中记录决策理由
4. **保留交叉引用** - 文档间的相互引用（如"详见「图片水印方案」"）应保持有效
5. **唯一来源原则** - 同一内容只在一处完整记录，避免重复或相似内容出现在不同文档或多处位置。如需引用，使用链接或交叉引用指向原文
6. **后台列表页原型统一规范** - 所有后台管理列表页的ASCII原型默认遵循统一布局顺序：`筛选区 → 标题与右侧操作按钮 → 表格区 → 分页区`
   - 筛选区单独占一行，筛选项从左到右排列，`[🔍 查询]` 与 `[C 重置]` 放在筛选区最右侧
   - 标题行单独占一行，左侧为页面标题，右侧为操作按钮；常见按钮包括 `新增`、`批量导入`、`下载模板`
   - 表格区不再额外套外层大边框，直接展示列表表头、数据行和底边框
   - 分页区单独放在表格下方，统一使用 `共XX条  XX条/页 ▼  <  1  前往 1 页  >` 风格
   - 新增后台列表页或调整既有列表页原型时，应优先复用 `项目管理`、`隐患库管理`、`法律法规库` 已采用的布局样式，避免同类页面出现不同骨架
7. **原型中不加入解释性文字** - ASCII原型仅展示实际界面的内容，不在原型内添加说明文字、标注或注释。如需解释，应在原型外的正文中描述

## 功能设计评审要求

当用户提出功能设计需求或提供设计方案时：

1. **必须进行评审** - 不能仅表示同意，需主动审视方案的合理性、完整性和潜在问题
2. **提出改进建议** - 如果有更优方案，应明确指出并提供具体建议
3. **关注风险点** - 指出可能遗漏的边界情况、技术难点或用户体验问题
4. **对比分析** - 如有多个可行方案，提供对比分析帮助决策

## 无构建/测试命令

本仓库仅包含Markdown文档，没有构建脚本、代码检查或测试命令。
