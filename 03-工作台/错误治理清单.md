# 错误治理清单

## 文档定位
- 本文档是仓库内“错误不再重复”的统一登记入口。
- 只记录已经抽象成可复用治理动作的错误类型，不写长篇过程叙事。
- 历史案例、原始决策原因和具体上下文仍归档到 [../90-归档记录/](../90-归档记录/)。

## 使用规则
- 同类错误第一次被指出后，必须登记到本清单，并决定先沉淀到 `Guide / Golden Path / Sensor / Ratchet` 的哪一层。
- 若同类错误再次出现，不能只口头纠正；必须升级治理动作，例如从规则升级到模板、从模板升级到脚本检查。
- 本文档中的“状态”只用于表示治理动作的推进情况，不替代业务主定义状态。

## 状态说明
| 状态 | 含义 |
|---|---|
| 仅记录 | 已识别错误类型，但还没有形成稳定治理动作 |
| 已加规则 | 已进入 README / 协议 / 清单 |
| 已加模板 | 已提供默认骨架、模板或起手路径 |
| 已加检查 | 已进入脚本传感器 |
| 已闭环 | 规则、默认路径和检查器至少覆盖到当前主要风险 |

## 错误类型索引

### 文档治理类
| 错误现象 | 触发场景 | 正确做法 | 升级层 | 当前状态 | 当前治理动作 | 案例/入口 |
|---|---|---|---|---|---|---|
| 稳定规则长期留在过程文档，未拆回正式主定义 | 需求沟通结束后直接收尾 | 将稳定规则拆回 PRD、产品架构、业务流程或归档记录 | Guide + Sensor | 已加检查 | 任务协议、完成检查、`check_workbench.py`、拆回正式文档清单 | [任务执行协议.md](任务执行协议.md) |
| 新犯过的错被当场纠正，但没有制度化动作 | 评审或收尾时发现问题 | 至少登记到本清单，并决定补规则、模板、脚本或评审门 | Ratchet | 已加规则 | 收尾要求必须回答“是否产生新的可复发错误，已如何制度化” | [任务完成检查.md](任务完成检查.md) |
| 页面或过程文档重复完整定义角色权限矩阵，导致权限主定义漂移 | 新增页面需求、评审记录或过程文档收口时顺手写角色到页面/操作权限映射 | 角色到页面/操作/数据范围权限的主定义统一回到 `系统管理.md`；页面和过程文档只写权限相关行为 | Guide + Sensor | 已加规则 | 任务执行协议、任务完成检查、`check_workbench.py` 轻量告警 | [../02-PRD文档/后台管理/系统管理.md](../02-PRD文档/后台管理/系统管理.md) |
| 页面需求沟通草图进入正式 PRD 后继续长期维护，导致草图与原型图双维护漂移 | 页面沟通阶段先写草图，后续原型稳定后又把草图留在正式 PRD 中 | 草图只用于沟通阶段收敛结构；正式 PRD 默认引用对应原型图文件，不再重复维护草图正文 | Guide + Ratchet | 已加规则 | 任务执行协议和任务完成检查约束“沟通保留草图，正式 PRD 引用原型图文件” | [任务执行协议.md](任务执行协议.md) |
| 页面章节保留不在本页展示的字段、重复导航或无信息增量说明，导致页面主定义变脏 | 用模板重构 PRD 时把历史字段、重复索引、权限/结果等上下文一起保留下来 | 只保留当前页面实际展示、实际操作、实际分支会用到的内容；不属于本页的字段回到对应页面，不增加信息量的重复说明直接删除 | Guide + Ratchet | 已加规则 | 页面级 PRD 收口时优先做“本页有效内容筛选”，发现冗余项直接删，不把历史残留继续带入新骨架 | `列表历史扩展字段如 施工单位 / 负责人 / 备注 / 创建时间 暂不进入当前主展示列表` |
| PRD 用“至少”“等”这类模糊下限词描述展示内容，导致研发不确定是否还有未写出的字段或元素 | 页面字段、图片区或摘要区收口时，用“至少展示图片名称、图片大小”“展示字段等”替代完整清单 | 页面展示内容应直接列完整、封闭的字段或元素清单；若只展示 `图片名称 / 图片大小`，就明确写这两项，不用“至少”“等”保留开放口径 | Guide + Ratchet | 已加规则 | 页面级 PRD 收口时，凡是展示清单都优先改成确定枚举；发现“至少/等/包括但不限于”这类开放口径，默认改成完整显式列举 | `每条至少展示 图片名称、图片大小` |
| 同一页面对同一对象写出互斥规则，导致研发无法判断最终行为 | 页面重构或规则搬运时，把不同语境下的约束和展示结果混写进同一页面 | 同一页面内，同一对象的状态、展示、操作阻断和弹窗结果必须能互相闭合；若对象不同，先拆对象，再分别定义 | Guide + Sensor + Ratchet | 已加规则 | 页面级 PRD 收口时，重点核对页面状态、字段展示、操作规则、弹窗结果是否对同一对象给出互斥描述；必要时用 `check_prd.py` 做窄规则告警 | `巡查记录至少保留 1 张图片` vs `图片列表无图片时展示空状态` |
| 本该由具体页面承接的规则被抽象挂到模块级，导致阅读路径绕远、页面定义不就地 | 模块 PRD 重构时为了提炼“通用规则”，把实际由某个页面触发或仅服务某页理解的规则上提到模块级 | 只有真正跨页面且必须唯一主定义的内容才保留在模块级；由具体页面触发、仅为页面展示或操作服务的规则，应下沉到对应页面章节就地定义 | Guide + Ratchet | 已加规则 | 页面级 PRD 收口时先判断“这是模块唯一口径，还是页面触发规则”；页面触发规则优先回到具体页面，不在模块级悬挂抽象说明 | `巡查记录生成规则`、`区域权限与项目选择规则` 被挂在模块级通用规则 |
| 因其他页面存在某按钮、入口或展示区，就在当前页面特意强调“没有”，导致页面规则冗余 | 页面重构时横向参考了其他页面，把当前页面未定义的元素反向写成否定句 | 当前页面未定义某元素、字段或操作，即默认不存在；不要因为其他页面存在同类元素，就在本页特意写“无按钮/无入口/无展示”。只有存在特殊歧义或必须阻断默认脑补时，才显式写“不提供/不展示/不支持” | Guide + Ratchet | 已加规则 | 页面级 PRD 收口时优先用“只写本页存在内容”表达页面结构；发现因横向对比而产生的纯否定型冗余描述直接删除 | `详情页为只读页面，无编辑按钮` |
| PRD 中的字段名称与原型图实际标签不一致，导致需求口径分裂 | 重构页面 PRD 时沿用旧字段名，未回看当前原型中的实际页面标签 | 页面级 PRD 中的字段名称应优先与当前原型图实际展示标签保持一致；若业务主定义已有统一命名，应先统一原型和 PRD，再避免两边各写一套叫法 | Guide + Ratchet | 已加规则 | 页面级 PRD 收口时，对照当前原型逐项核字段标签；发现 `负责人姓名/负责人`、`项目地址/施工地址` 这类分裂命名时优先统一口径 | `负责人姓名 / 负责人`、`项目地址 / 施工地址` |

### 原型实现类
| 错误现象 | 触发场景 | 正确做法 | 升级层 | 当前状态 | 当前治理动作 | 案例/入口 |
|---|---|---|---|---|---|---|
| 已有公共组件未复用，重新手写平行实现 | 新增后台列表页、详情页、弹窗页 | 先列“页面元素 -> 组件名”映射，再优先使用共享组件和模板 | Guide + Golden Path + Sensor | 已加检查 | 组件接入契约、起手模板、`check_prototypes.py` | [../04-原型效果图/后台管理/assets/README.md](../04-原型效果图/后台管理/assets/README.md) |
| 新建模板 HTML 时脱离现有原型页语境，做成抽象 demo 或缺少实际样式结构 | 新增列表页、详情页、弹窗页起手模板 | 先参考最接近的已有原型图页面，再抽取成模板；优先复用真实页面的容器、布局、卡片和操作区骨架，不从空白猜结构 | Guide + Golden Path | 已加规则 | 模板创建优先从已有原型页对齐，再做最小抽象；避免先写通用 demo 再回头修正 | [../04-原型效果图/后台管理/assets/templates/README.md](../04-原型效果图/后台管理/assets/templates/README.md) |
| 页面私有样式覆盖共享布局/状态/底部操作栏规则 | 为了赶页面直接写 sticky/fixed/自定义状态栏 | 优先复用 `page-actions`、`prototype-switcher.js`、`prototype-layout.js`，私有样式只保留业务特化部分 | Guide + Sensor | 已加检查 | 原型资源 README 禁止项、原型检查脚本 | [../04-原型效果图/后台管理/assets/README.md](../04-原型效果图/后台管理/assets/README.md) |
| 筛选字段被自定义网格拉得过宽，导致列表页头部松散失衡 | 自定义筛选区时用 `fr` 拉伸输入框/级联框 | 2-4 个筛选字段优先使用固定宽度带，字段建议落在 180-240px，查询/重置按钮保持 `auto` | Guide + Golden Path + Sensor | 已加检查 | 原型资源 README、列表页起手模板、`check_prototypes.py` 轻量告警 | [../04-原型效果图/后台管理/assets/README.md](../04-原型效果图/后台管理/assets/README.md) |

### 需求沟通类
| 错误现象 | 触发场景 | 正确做法 | 升级层 | 当前状态 | 当前治理动作 | 案例/入口 |
|---|---|---|---|---|---|---|
| 直接给方案或页面结果，没有先做评审和待确认项沉淀 | 新功能沟通、页面收口 | 先评审合理性，再输出过程骨架、待确认项和拆回清单 | Guide + Golden Path | 已加规则 | 任务执行协议、需求沟通输出框架、评审记录骨架 | [任务执行协议.md](任务执行协议.md) |

## 默认升级顺序
1. 第一次出现：登记错误类型，并补最小正确规则。
2. 第二次出现：为高频场景补模板、骨架或默认起手路径。
3. 第三次出现或明显高频：补脚本检查，转成可见背压。
4. 若仍无法稳定避免：补评审强关口或人工确认门。

## 本轮优先覆盖面
- 文档治理：主定义漂移、拆回不完整、收尾未闭环。
- 原型实现：公共组件未复用、页面私有平行实现、未接入共享脚本。
- 需求沟通：未先评审、未沉淀待确认项、错误未制度化。
