# 错误治理清单

## 文档定位
- 仓库”错误不再重复”的统一登记入口，只记录已抽象为可复用治理动作的错误类型。
- 历史案例、决策原因和上下文归档至 [../90-归档记录/](../90-归档记录/)。

## 使用规则
- 同类错误首次出现后，登记到本清单并决定沉淀到 `Guide / Golden Path / Sensor / Ratchet` 的哪一层。
- 同类错误再次出现时，必须升级治理动作（规则→模板→脚本检查），不能只口头纠正。
- 已登记错误同时用于后续同类任务的编辑前避错检查：先按”编辑前必查信号”和”默认处理动作”自检，再落笔。
- 本文档”状态”仅表示治理动作推进情况，不替代业务主定义状态。

## 状态说明
| 状态 | 含义 |
|---|---|
| 仅记录 | 已识别错误类型，但还没有形成稳定治理动作 |
| 已加规则 | 已进入 README / 协议 / 清单 |
| 已加模板 | 已提供默认骨架、模板或起手路径 |
| 已加检查 | 已进入脚本传感器 |
| 已闭环 | 规则、默认路径和检查器至少覆盖到当前主要风险 |

## 错误类型索引

### 文档治理类
- `流程闭环类`：优先处理稳定规则是否拆回正式主定义、错误是否完成制度化。
- `页面内容收口类`：优先处理页面主定义是否就地、完整、闭合且可实现。
| 错误现象 | 触发场景 | 正确做法 | 升级层 | 当前状态 | 当前治理动作 | 检查方式 | 编辑前必查信号 | 默认处理动作 | 案例/入口 |
|---|---|---|---|---|---|---|---|---|---|
| 稳定规则留在过程文档，未拆回正式主定义 | 需求沟通后直接收尾 | 将稳定规则拆回 PRD、产品架构、业务流程或归档记录 | Guide + Sensor | 已加检查 | 任务协议、完成检查、`check_workbench.py`、拆回正式文档清单 | 协议前置 + 脚本可检 | 本轮新增内容已明确为稳定规则，且当前承载文档仍是需求沟通或评审记录等过程文档 | 更新”拆回清单”，将稳定规则拆回正式主定义；过程文档只留摘要、原因和链接 | [任务执行协议.md](任务执行协议.md) |
| 新错误被当场纠正，但未制度化 | 评审或收尾时发现问题 | 至少登记到本清单，并决定补规则、模板、脚本或评审门 | Ratchet | 已加规则 | 收尾时必须回答”是否产生新的可复发错误，已如何制度化” | 协议前置 + 人工评审 | 本轮出现”上次纠正过但协议/模板/脚本未接住”的同类错误 | 先修当前结果，再明确补哪一层治理动作；当轮至少登记，不允许只停留在对话 | [任务完成检查.md](任务完成检查.md) |
| 页面或过程文档重复定义完整角色权限矩阵，导致权限主定义漂移 | 新增页面或过程文档收口时顺手写角色到页面/操作权限映射 | 角色权限主定义统一回到 `系统管理.md`；页面和过程文档只写权限相关行为 | Guide + Sensor | 已加规则 | 任务执行协议、任务完成检查、`check_workbench.py` 轻量告警 | 协议前置 + 人工评审 | 准备写”谁能看/谁能改/哪个角色有哪个按钮”等完整角色矩阵 | 先判断是主定义变更还是页面表现；主定义回写 `系统管理.md`，页面 PRD 只保留权限行为 | [../02-PRD文档/后台管理/系统管理.md](../02-PRD文档/后台管理/系统管理.md) |
| 沟通草图进入正式 PRD 后继续维护，导致草图与原型双维护漂移 | 沟通阶段先写草图，原型稳定后未从正式 PRD 中移除 | 草图仅用于沟通阶段收敛结构；正式 PRD 引用原型图文件，不重复维护草图正文 | Guide + Ratchet | 已加规则 | 任务执行协议和完成检查约束”沟通保留草图，正式 PRD 引用原型图” | 协议前置 + 脚本可检 | 正在修改正式 PRD，且页面结构仍用 ASCII 草图而非原型链接 | 沟通阶段保留草图；拆回正式 PRD 时删草图正文，改为 `对应原型` 链接 | [任务执行协议.md](任务执行协议.md) |
| 页面保留不属于本页的字段、历史残留说明、反向定义未展示内容或无信息增量的重复说明，导致页面主定义变脏 | 用模板重构 PRD、横向参考其他页面时，把历史字段、重复索引、本页未定义元素一并保留 | 只保留本页实际展示、操作、分支用到的内容；不属于本页的字段回到对应页面；当前页面未定义即默认不存在；无信息增量的重复说明直接删除 | Guide + Ratchet | 已加规则 | 页面 PRD 收口时做”本页有效内容筛选”，发现冗余直接删，不把历史残留或横向对比产生的否定描述带入新骨架 | 协议前置 + 脚本可检 | 准备保留历史字段、扩展字段、重复导航，写”当前页暂不展示但先记录””无按钮””无入口””无展示”，或在字段表里写 `不展示项` / `不提供项` / `不支持项` | 先做”本页有效内容筛选”；字段表只能列本页真实展示字段，若只是说明未展示内容，默认删除，不改写成反向字段；纯否定描述默认删除，仅在有明确歧义时保留”不提供/不展示/不支持”并写清歧义点；其余内容下沉回承载页面或删除 | `列表历史扩展字段如 施工单位 / 负责人 / 备注 / 创建时间 暂不进入当前主展示列表`、`详情页为只读页面，无编辑按钮`、`| 不展示项 | ... |` |
| 本应由弹窗/抽屉内容区承接的字段或内部操作规则被挂在页面外层，导致主定义层级错挂 | 收口页面时把本应在弹窗/抽屉内定义的按钮、字段、内部操作单独挂在页面操作规则或字段区 | 页面外层只保留打开入口和页面级结果；弹窗/抽屉内字段和内部操作规则回到对应主定义 | Guide + Ratchet | 已加规则 | 页面 PRD 收口时新增”主定义层级判断”，区分页面级规则与弹窗/抽屉内部规则 | 协议前置 + 人工评审 | 页面外层出现本应由弹窗/抽屉承接的按钮、字段、内部操作细节，且对应弹窗/抽屉章节已存在或应存在 | 先判断规则属于页面外层还是弹窗/抽屉内部；内部规则下沉回对应弹窗/抽屉主定义，页面外层只保留入口、结果和影响范围 | `创建任务批次弹窗` 内的 `自动分配人员` 被单独挂在页面操作规则 |
| 页面已有可见且可点击的核心控件或入口，但 PRD 未定义对应操作规则，导致页面可见行为缺失 | 收口页面时只写了字段与展示规则，默认把状态卡片、Tab、图片卡片、列表项、链接等交互当成不言自明 | 凡是页面中用户可点击且会引起状态切换、列表刷新、跳转、弹窗、抽屉或结果变化的控件，都必须在就近的 `操作规则` 中定义 | Guide + Ratchet | 已加规则 | 页面 PRD 收口时，回看原型中的状态卡片、Tab、图片卡片、列表项、链接等可点击元素，逐项补齐对应操作规则 | 协议前置 + 人工评审 | `字段与展示规则` 中已经出现状态卡片、Tab、列表项、图片卡片、链接、按钮等可点击元素，但 `操作规则` 中没有对应条目 | 逐个回看原型中的可点击元素，补齐对应操作规则；页面级控件写在页面操作规则，弹窗/抽屉内控件写在对应弹窗/抽屉章节 | `任务分配管理` 页已有状态卡片，但漏写 `点击状态卡片` 的 `状态切换` 规则 |
| PRD 用”至少””等”模糊词描述展示内容，导致研发不确定是否还有未写出的字段 | 页面收口时用”至少展示””字段等”替代完整清单 | 展示内容直接列完整封闭的字段清单；只展示 `图片名称 / 图片大小` 就写这两项，不用”至少””等”保留开放口径 | Guide + Ratchet | 已加规则 | 页面 PRD 收口时，展示清单优先改成确定枚举；发现”至少/等/包括但不限于”默认改成完整显式列举 | 协议前置 + 脚本可检 | 行文出现”至少展示””字段等””内容包括但不限于”等开放表达，且上下文描述展示项或规则清单 | 改成封闭枚举，完整列出要展示/填写/校验的字段，不给研发留”还有未写出内容”空间 | `每条至少展示 图片名称、图片大小` |
| 同一页面对同一对象写出互斥规则，研发无法判断最终行为 | 页面重构或规则搬运时，不同语境的约束和展示结果混写进同一页面 | 同一页面内，同一对象的状态、展示、操作阻断和弹窗结果必须互相闭合；若对象不同，先拆对象再分别定义 | Guide + Sensor + Ratchet | 已加规则 | 页面 PRD 收口时，核对同一对象的状态/展示/操作规则/弹窗结果是否互斥；必要时用 `check_prd.py` 告警 | 协议前置 + 脚本可检 | 同一对象在不同段落出现互斥状态、展示、操作阻断或结果反馈 | 先做”同对象闭合检查”；同一对象保留一套闭合规则；不同对象先拆边界再分别定义 | `巡查记录至少保留 1 张图片` vs `图片列表无图片时展示空状态` |
| 应由具体页面承接的规则被抽象挂到模块级，导致阅读绕远、页面定义不就地 | 模块 PRD 重构时为提炼”通用规则”，把实际仅服务某页的规则上提到模块级 | 只有真正跨页面且必须唯一主定义的内容才保留在模块级；页面触发规则下沉到对应页面就地定义 | Guide + Ratchet | 已加规则 | 页面 PRD 收口时判断”模块唯一口径还是页面触发规则”；页面触发规则回到具体页面 | 协议前置 + 脚本可检 | 规则描述出现按钮、弹窗、抽屉、列表项、确认、提交、入池、页面内阻断等页面触发语义 | 判定”模块唯一口径 / 页面触发规则”；依赖页面入口、按钮、弹窗、列表项或页面阻断条件的规则默认下沉对应页面，只有跨页面复用且必须唯一主定义时才保留模块级 | `巡查记录生成规则`、`区域权限与项目选择规则` 被挂在模块级通用规则 |
| PRD 字段名与原型图标签不一致，导致需求口径分裂 | 重构页面 PRD 时沿用旧字段名，未回看当前原型中的实际标签 | 页面 PRD 字段名应与原型图标签保持一致；若业务已有统一命名，先统一原型和 PRD | Guide + Ratchet | 已加规则 | 页面 PRD 收口时对照原型逐项核查字段标签；发现 `负责人姓名/负责人` 等分裂命名时统一口径 | 协议前置 + 人工评审 | 页面字段、筛选项、按钮名、弹窗标题准备落笔，但未回看原型或现有统一命名 | 落笔前对照原型标签和业务主定义统一命名；发现分裂先统一 PRD 与原型，再展开规则 | `负责人姓名 / 负责人`、`项目地址 / 施工地址` |

### 原型实现类
| 错误现象 | 触发场景 | 正确做法 | 升级层 | 当前状态 | 当前治理动作 | 案例/入口 |
|---|---|---|---|---|---|---|
| 已有公共组件未复用，平行手写实现 | 新增后台列表页、详情页、弹窗页 | 先列”页面元素→组件名”映射，优先使用共享组件和模板 | Guide + Golden Path + Sensor | 已加检查 | 组件接入契约、起手模板、`check_prototypes.py` | [../04-原型效果图/后台管理/assets/README.md](../04-原型效果图/后台管理/assets/README.md) |
| 新建模板 HTML 脱离现有原型页语境，做成抽象 demo 或缺少实际样式结构 | 新增列表页、详情页、弹窗页起手模板 | 先参考最接近的已有原型页面再抽取模板；优先复用真实页面的容器、布局、卡片和操作区骨架，不从空白猜结构 | Guide + Golden Path | 已加规则 | 模板创建从已有原型页对齐再做最小抽象，避免先写通用 demo 再修正 | [../04-原型效果图/后台管理/assets/templates/README.md](../04-原型效果图/后台管理/assets/templates/README.md) |
| 页面私有样式覆盖共享布局/状态/底部操作栏规则 | 为赶页面直接写 sticky/fixed/自定义状态栏 | 优先复用 `page-actions`、`prototype-switcher.js`、`prototype-layout.js`，私有样式只保留业务特化部分 | Guide + Sensor | 已加检查 | 原型资源 README 禁止项、原型检查脚本 | [../04-原型效果图/后台管理/assets/README.md](../04-原型效果图/后台管理/assets/README.md) |
| 筛选字段被自定义网格拉得过宽，列表页头部松散失衡 | 自定义筛选区时用 `fr` 拉伸输入框/级联框 | 2-4 个筛选字段优先用固定宽度带，字段 180-240px，查询/重置按钮 `auto` | Guide + Golden Path + Sensor | 已加检查 | 原型资源 README、列表页起手模板、`check_prototypes.py` 告警 | [../04-原型效果图/后台管理/assets/README.md](../04-原型效果图/后台管理/assets/README.md) |

### 需求沟通类
| 错误现象 | 触发场景 | 正确做法 | 升级层 | 当前状态 | 当前治理动作 | 案例/入口 |
|---|---|---|---|---|---|---|
| 直接给方案或页面结果，未先做评审和待确认项沉淀 | 新功能沟通、页面收口 | 先评审合理性，再输出过程骨架、待确认项和拆回清单 | Guide + Golden Path | 已加规则 | 任务执行协议、需求沟通输出框架、评审记录骨架 | [任务执行协议.md](任务执行协议.md) |

## 默认升级顺序
1. 首次出现：登记错误类型，补最小正确规则。
2. 再次出现：对高频场景补模板、骨架或默认起手路径。
3. 三次以上或明显高频：补脚本检查，转成可见背压。
4. 仍无法稳定避免：补评审强关口或人工确认门。

## 本轮优先覆盖面
- 文档治理：主定义漂移、拆回不完整、收尾未闭环
- 原型实现：公共组件未复用、私有平行实现、未接入共享脚本
- 需求沟通：未先评审、未沉淀待确认项、错误未制度化
