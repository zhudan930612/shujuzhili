# AGENTS.md

本文件定义本仓库内的默认工作规则。

## 仓库概览

这是一个纯文档仓库，用于记录数据治理平台的产品设计文档，服务于小散工程安责险事故预防服务业务。

- 主要内容：Markdown 文档、HTML 原型效果图、Excel 模板
- 默认语言：简体中文
- 仓库内没有构建脚本、代码检查或测试命令

## 处理设计需求时的默认行为

当用户提出功能设计需求、页面方案或产品方案时：

1. 必须进行评审，不能只表示同意。
2. 需要主动指出合理性、完整性、边界条件和潜在问题。
3. 若存在更优方案，需要明确给出替代建议。
4. 若存在多个可行方案，需要做对比分析，帮助决策。

## 关键文件与目录

| 文件/目录 | 用途 |
|-----------|------|
| `数据治理平台-上下文.md` | 业务背景、战略定位、价值链分析、风险评估 |
| `流程图决策.md` | 产品决策、流程设计、UI/UX 规范、ASCII 原型 |
| `知识库体系设计.md` | 隐患识别报告的知识库架构 |
| `数据治理平台-Mermaid.mmd` | Mermaid 流程图 |
| `后台管理原型效果图/` | 高保真 HTML 原型，含首页导航入口 |
| `模板文件/` | 批量导入 Excel 模板 |

## 核心术语

| 术语 | 含义 |
|------|------|
| **安责险** | 安全生产责任保险（高危行业强制投保） |
| **小散工程** | 小型、分散工程项目，如装修、维修等 |
| **巡查员** | 现场执行巡查、采集隐患照片的人员 |
| **S1-S8** | 系统阶段：项目来源 → 采集 → 汇聚 → 多轮标注 → 质检 → 数据池 → AI 训练 |
| **标注** | 对隐患图片进行标注、分类、关联法规的数据处理工作 |
| **备案系统** | 政府侧建设工程项目备案管理系统 |

## 文档编辑规则

1. **保持一致性**
   - 修改 `流程图决策.md` 中的流程时，应同步更新 `数据治理平台-Mermaid.mmd`。

2. **遵循现有格式**
   - 规格说明优先用表格。
   - ASCII 原型优先用现有骨架风格。
   - 文档结构优先用层级标题组织。

3. **更新决策记录**
   - 更改产品决策时，应在对应决策章节记录决策理由。

4. **保留交叉引用**
   - 文档间已有的“详见某章节/某方案”引用应保持有效。

5. **唯一来源原则**
   - 同一内容只在一处完整定义。
   - 其他位置只能引用、沿用或补差异，不能重复完整展开。

6. **规则定义唯一来源规范**
   - 业务口径、交互规则、字段说明、ASCII 骨架、示例场景，都应有且仅有一个主定义区。
   - 业务规则优先放在最靠近判断逻辑的章节，如“判定规则”“规则定义”。
   - 模块综述、页面摘要、后续章节默认只做引用，不重复定义。
   - 同一页面的 ASCII 原型不要重复画多个近似版本；如必须出现多个版本，必须明确标注为不同状态。
   - 同一字段或状态在不同表格中复用时，应优先引用主定义；如有差异，只写增量差异。
   - 示例或场景只在最能承载规则的主章节完整展开，其他位置避免重复举例。

7. **原型中不加入解释性文字**
   - ASCII 原型只展示真实界面内容。
   - 原型内不加入说明文字、标注或注释；如需解释，应写在原型外正文中。

## 后台列表页 ASCII 原型规则

所有后台管理列表页的 ASCII 原型默认遵循统一布局顺序：

`筛选区 → 标题与右侧操作按钮 → 表格区 → 分页区`

- 筛选区单独占一行，筛选项从左到右排列，`[🔍 查询]` 与 `[C 重置]` 放在最右侧。
- 标题行单独占一行，左侧为页面标题，右侧为操作按钮。
- 常见操作按钮包括：`新增`、`批量导入`、`下载模板`。
- 表格区不额外套大边框，直接展示表头、数据行和底边框。
- 分页区统一放在表格下方，使用统一风格。
- 新增或调整后台列表页时，应优先复用 `项目管理`、`隐患库管理`、`法律法规库` 已采用的布局样式。

## 子页与弹窗按钮规则

- 子页面（如抽屉页、侧边展开页）和弹窗的按钮统一放置在右下角。
- 页面级底部操作栏按当前共享组件规则执行。

## 后台 HTML 原型效果图规则

### 1. 视觉基线

为本项目生成后台管理系统的 HTML / SVG 原型效果图时，默认使用当前已确认的蓝白后台风格，不随意切换视觉体系。

- 整体结构：`顶部深蓝导航 + 左侧白色菜单 + 中部浅灰工作区 + 白色内容卡片`
- 顶部导航：深蓝渐变、白字、一级功能横向排列、右上角用户信息
- 左侧菜单：白底菜单，当前模块蓝底高亮
- 内容区：默认包含面包屑、页签/模块标题、筛选区、列表卡片、表格、分页
- 默认桌面后台画布：`1920 × 1080 px`

### 2. 风格约束

- 表格、筛选器、按钮、分页等组件应偏业务系统风格，强调规整、稳定、清晰。
- 禁止做营销页、站点页、海报页式设计。
- 真实界面内容与功能说明必须分层；如需展示说明，优先用抽屉、浮窗、侧边说明板，不把说明塞进界面主体。
- 配色、间距、控件风格应优先延续用户已确认的参考图气质，保证同一项目风格统一。

### 3. 组件优先原则

生成或修改 `docs/06-原型效果图/后台管理/` 下的原型页面时，必须优先复用已有公共组件。

- 处理后台原型页时，优先使用 `$prototype-component-first` 技能，并按该技能定义的“组件优先”工作流执行。
- 生成原型前，优先检查并复用以下公共组件来源：
  - `docs/06-原型效果图/后台管理/assets/prototype.css`
  - `docs/06-原型效果图/后台管理/assets/prototype.js`
  - `docs/06-原型效果图/后台管理/assets/README.md`
  - `docs/06-原型效果图/后台管理/后台管理-组件预览.html`
- 已有组件能够满足需求时，禁止重复手写同类 UI、页面私有样式或平行实现。
- 如遇到现有组件无法覆盖的新通用结构，应先沉淀为公共组件，再接入具体页面。
- 页面级私有实现只允许用于明显业务特化、且确认不具备复用价值的内容。

### 4. 参考图执行规则

当用户提供参考图片时，必须将参考图视为唯一视觉基准，禁止擅自“优化”或“推断”。

- 先严格复刻结构、层级、布局关系，不做“我觉得更好”的调整。
- 只有用户明确要求“优化 / 重设计 / 换风格”时，才允许设计发挥。
- 如果参考图与文档冲突，先指出冲突点，不自行脑补。
- 执行顺序固定为：`先复刻 → 有明确优化指令才调整 → 有冲突先确认再执行`。

## Excel 模板规则

生成新的批量导入 Excel 模板时，必须参考 `模板文件/` 下已有模板的：

- Sheet 结构
- 表头底色
- 边框风格
- `填写说明` Sheet 格式

目标是保持同一项目下模板风格统一。
